# DEPRECATED DECISIONS (Do Not Treat as Current Product Requirements)

## Purpose

This file enumerates legacy decisions and assumptions that must **not** drive current implementation or roadmap execution unless explicitly re-approved.

| Old decision | Why it existed | What supersedes it | Current canonical source | Historical code still exists? |
| --- | --- | --- | --- | --- |
| “Original OCR file remains unstored.” | Reduced payload expectations and quick MVP scope in early OCR phase. | `Document Vault retains source evidence` and immutable source lineage in `PRODUCT_CONTRACT.md`. | `PRODUCT_CONTRACT.md`, `FEATURE_REGISTRY.md` (Document Vault), `ROADMAP.md` Phase 2. | No direct implementation gap should remain in future slices; some legacy code paths may still assume missing source. |
| Mandatory PTI for all users/workloads | Early compliance-hardening approach with minimal policy controls. | Configurable PTI policy: optional personal PTI, policy-required PTI for fleet/carrier, scheduled weekly photo PTI. | `PRODUCT_CONTRACT.md`, `FEATURE_REGISTRY.md` (PTI, Weekly photo PTI), `ROADMAP.md` Phase 3. | Test and UX paths may still encode hard-gate assumptions. |
| First-truck fallback on ambiguous load assignment | Intended to avoid blocking UI states in early load workflows. | Deterministic assignment + explicit ambiguity handling; “no first-truck fallback” contract. | `PRODUCT_CONTRACT.md`, merged PR context (#95), open load issue tracking. | Legacy behavior may still be partially present; should be treated as technical debt. |
| Service-worker version as behavior key | Earlier workaround for CDN/cache instability. | Versioning remains useful operationally, but it is not a product contract; behavior depends on persisted data correctness and restore invariants. | `PRODUCT_CONTRACT.md`, collaboration architecture notes. | Old cache-bust habits can still appear in docs/test histories. |
| Fabricated or non-canonical module maps | Historical migration aid for UI composition. | Canonical identity/data-layer model and role/workspace/capability boundaries. | `PRODUCT_CONTRACT.md`, `DOCUMENTATION_AUTHORITY.md`. | No runtime requirement; artifacts may remain as references. |
| Abandoned IA pages `page-work`, `page-truck`, `page-money`, `page-team`, `page-marketplace` as active surfaces | Prior IA experiments and route planning hypotheses. | Keep as deprecated/deprioritized; no active product intent to ship these surfaces in current architecture. | `DEPRECATED_DECISIONS.md` (this file), `ROADMAP.md` Phase 7, `FEATURE_REGISTRY.md` (Team/Marketplace/Community). | Files/route history may remain; no deletion in this slice. |
| Direct AI-to-database write patterns in SIDR workstreams | Early SIDR exploration lacked governance layer. | SIDR Core constrained to service-mediated CrewBIQ integration and governance. | `PRODUCT_CONTRACT.md`, `DEPRECATED_DECISIONS.md`, collaboration SIDR constraints. | Legacy references remain in docs/research repos; implementation must be governed before re-enabling. |
| Base44 as mandatory runtime migration | Design acceleration experiment with unclear dependency guarantees. | Base44 is optional design reference; product/runtime can proceed without it. | `PRODUCT_CONTRACT.md`, collaboration issue #90 review notes. | Design references remain in docs, but not runtime dependency. |
| “Monolithic index.html” as immediate blocker for progress | Assumed only decomposed UI could be safely improved. | Monolith is accepted technical debt; decompose only through explicit slices while preserving behavior. | `PRODUCT_CONTRACT.md` and ongoing Slice 0b scope note. | Current `index.html` runtime remains. |
| Open docs-only roadmap fragments as executable plan | Legacy markdown files and PR drafts were fragmented across repos. | Canonical registry/roadmap documents in `docs/product` are now authoritative for plan status. | `DOCUMENTATION_AUTHORITY.md`, this file, `ROADMAP.md`. | Historical docs remain as evidence only. |
