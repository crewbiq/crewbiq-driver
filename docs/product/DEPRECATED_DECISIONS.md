# DEPRECATED DECISIONS (Do Not Treat as Current Product Requirements)

## Purpose

This file enumerates legacy decisions and assumptions that must **not** drive current implementation or roadmap execution unless explicitly re-approved.

| Old decision | Why it existed | What supersedes it | Current canonical source | Historical code still exists? |
| --- | --- | --- | --- | --- |
| “Original OCR file remains unstored.” | Reduced payload expectations and quick MVP scope in early OCR phase. | Document Vault requirement in `PRODUCT_CONTRACT.md` requiring source retention, hash/provenance, and audit linkage. | `PRODUCT_CONTRACT.md`, `FEATURE_REGISTRY.md` (Document Vault), `ROADMAP.md` Phase 2. | OCR UI still says “not stored.” The missing-implementation behavior remains to be implemented, not a completed feature. |
| Mandatory PTI for everyone | Early hardening assumed all users require PTI with uniform behavior. | PTI is configurable and scoped by workspace/fleet policy; personal PTI remains optional where policy allows. | `PRODUCT_CONTRACT.md`, `ROADMAP.md` Phase 3, `FEATURE_REGISTRY.md` (PTI). | Some test/UX paths still assume hard-gate behavior in places. |
| First-truck fallback on ambiguous load assignment | Avoided blocking states in early load workflow implementation. | No first-truck fallback when assignment is ambiguous. | `PRODUCT_CONTRACT.md`, merged PR context (#95), legacy load issues. | Legacy logic may remain in some branches; runtime behavior is contract-controlled by canonical extraction plan. |
| Service-worker version as product behavior | Cache-version checks were used as a behavior anchor workaround. | Behavior contracts are now load-order and domain correctness anchored, not cache-version text comparison. | `PRODUCT_CONTRACT.md`, collaboration notes | `sw.js` version literals remain in runtime code as operational config only. |
| Fabricated/non-existent module-map entries as active plan truth | Module extraction plans sometimes referenced files not present in repo. | Architecture maps now reference only existing artifacts; absent files are treated as planned/deprecated only when evidence supports it. | `DOCUMENTATION_AUTHORITY.md`, `FUNCTIONAL_AUDIT.md`, `ARCHITECTURE.md` | None in code as module files; only stale text artifacts if any were removed/updated. |
| Abandoned IA pages `page-work`, `page-truck`, `page-money`, `page-team` as active targets | They were part of a dead domain-hub experiment before current audit scope. | Treat these pages as dead/abandoned routing targets until an explicit product decision opts to resume the IA. | `ARCHITECTURE.md`, `LEGACY_ARTIFACT_MATRIX.md`, `ROADMAP.md` Phase 7. | Historical HTML/JS fragments remain in `index.html`; no new behavior is required in this slice. |
| “page-marketplace is a product market feature” as direct implementation truth | The marketplace shell once looked like a product destination. | Legacy `page-marketplace` implementation is deprecated technical shell; strategic marketplace concept is a separate future decision. | `FEATURE_REGISTRY.md` (Marketplace), `ROADMAP.md` Phase 7, `DEPRECATED_DECISIONS.md` | `page-marketplace` renderer remains in `index.html` as dead shell only. |
| Broad “community” product surface in runtime | Earlier documentation mixed Links runtime container with strategic community product planning. | `page-community` is preserved for Links until migration + contract testing; broader community concept is deferred, not removed, and remains a future strategic decision. | `FEATURE_REGISTRY.md` (Links, Community), `PRODUCT_CONTRACT.md`, `ROADMAP.md`. | `index.html` supports `page-community` and `renderCommunity`; do not remove until Links extraction is complete. |
| Direct AI-to-database write patterns for SIDR workstreams | Early SIDR ideas lacked governance and service boundaries. | SIDR integration must be constrained to CrewBIQ service/tooling boundaries and no direct DB superuser writes. | `PRODUCT_CONTRACT.md`, `DEPRECATED_DECISIONS.md`, collaboration SIDR constraints. | References remain in docs/research. |
| Base44 as mandatory runtime migration | Design-prototype assumption imported as required architecture. | Base44 is optional visual reference only; runtime migration is not mandatory. | `PRODUCT_CONTRACT.md`, `FEATURE_REGISTRY.md` (Base44 redesign path). | Design references remain; no runtime requirement. |
| Mobile web + native store packaging conflated | Some past status entries treated mobile presence as native store packaging. | Mobile web usability and offline packaging are active context; Play Store / App Store packaging is not a current implemented requirement. | `FEATURE_REGISTRY.md` (Mobile packaging), `ROADMAP.md`. | No native packaging CI, manifest, or release pipeline evidence in this repo. |
| Open roadmap/archival docs as current behavior | Legacy markdown and PR fragments were being interpreted as executed implementation targets. | Canonical status artifacts in this folder are now the planning truth; legacy docs remain evidence only. | `DOCUMENTATION_AUTHORITY.md`, `ROADMAP.md`, `FEATURE_REGISTRY.md` | Historical issue/ADR artifacts remain as historical evidence. |
